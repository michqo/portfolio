<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Dark background -->
  <rect width="32" height="32" fill="#171717"/>
  <!-- Subtle dot grid -->
  <defs>
    <pattern id="dots" x="0" y="0" width="8" height="8" patternUnits="userSpaceOnUse">
      <circle cx="4" cy="4" r="0.65" fill="rgba(255,255,255,0.07)"/>
    </pattern>
  </defs>
  <rect width="32" height="32" fill="url(#dots)"/>
  <!-- Emerald left accent bar -->
  <rect x="0" y="0" width="3" height="32" fill="#34d399"/>
  <!-- / in emerald -->
  <text x="7" y="22" font-family="ui-monospace,monospace" font-size="15" font-weight="700" fill="#34d399">/</text>
  <!-- m in white -->
  <text x="16" y="22" font-family="ui-monospace,monospace" font-size="15" font-weight="700" fill="#ffffff">m</text>
</svg>
